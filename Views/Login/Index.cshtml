@model EmployeePerformanceSystem.Models.User

@{
    ViewData["Title"] = "Login";
}
<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title>Employee Login</title>
    <link href="~/css/css3.css" rel="stylesheet">
    <style>
        /* Base styling */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'B Nazanin', serif; /* فونت B Nazanin */
        }

        /* Login container */
        .login-wrapper {
            background: white;
            border-radius: 16px;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1), 0 4px 6px rgba(0, 0, 0, 0.08);
            width: 100%;
            max-width: 420px;
            padding: 40px;
            transition: transform 0.3s ease;
        }

        .login-wrapper:hover {
            transform: translateY(-4px);
        }

        /* Header styling */
        .login-header {
            text-align: center;
            margin-bottom: 35px;
        }

        .login-header h2 {
            color: #2c3e50;
            font-weight: 700;
            letter-spacing: -0.5px;
            font-size: 28px;
        }

        /* Input fields */
        .form-input {
            width: 100%;
            padding: 16px;
            margin: 12px 0;
            border: 2px solid #ecf0f1;
            border-radius: 10px;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .form-input:focus {
            border-color: #3498db;
            outline: none;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.2);
        }

        /* Button styling */
        .btn-login {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
            border: none;
            border-radius: 10px;
            color: white;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 25px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .btn-login:hover {
            background: linear-gradient(135deg, #2980b9 0%, #2c3e50 100%);
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        /* Additional links */
        .login-footer {
            text-align: center;
            margin-top: 25px;
        }

        .login-footer a {
            color: #3498db;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .login-footer a:hover {
            color: #2980b9;
            text-decoration: underline;
        }

        /* Error message styling */
        .error-message {
            background: #ffebee;
            color: #c62828;
            padding: 14px;
            border-radius: 10px;
            margin-bottom: 20px;
            text-align: center;
            font-size: 14px;
        }
    </style>
</head>

<body>
    <div class="login-wrapper">
        <div class="login-header">
            <h2>خوش آمدید</h2>
        </div>

        @if (ViewBag.ErrorMessage != null)
        {
            <div class="error-message">@ViewBag.ErrorMessage</div>
        }

        <form method="post" asp-action="Authenticate">
            <input type="text" class="form-input" name="username" placeholder="نام کاربری" required>
            <input type="password" class="form-input" name="password_hash" placeholder="کلمه عبور" required>
            <button type="submit" class="btn-login">ورود</button>
        </form>
    </div>
</body>

</html>